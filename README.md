# server_body_parser


This expose http://localhost:3000/ service
and any request to http://localhost:8888 (You can mimic your calls to it)
will handle the request to show what was sent.

Edit the method `expectedReturn` in the `BodyParser.js` file if you need
a temporal different response
